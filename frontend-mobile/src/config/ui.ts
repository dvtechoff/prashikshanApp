/**
 * UI Configuration
 * 
 * Central configuration for UI-related settings across the app.
 * Change these values to toggle features app-wide.
 */

export const UI_CONFIG = {
  /**
   * Show or hide the header/title bar on all screens
   * 
   * true = Headers are visible (shows screen titles like "Applications", "Dashboard", etc.)
   * false = Headers are hidden (edge-to-edge content)
   */
  SHOW_HEADERS: true,
} as const;
